# Harden: H2 Implementation

Hardening pass on commit `0182185` (H2 implementation).

## Effort/Impact Table

| # | Opportunity | Effort | Impact | Action |
|---|-------------|--------|--------|--------|
| 1 | Reorder h2 before quic/h3 in all feature/member lists | Quick | Medium | Auto-fix |
| 2 | Deduplicate SendData handler in connection.rs | Easy | Medium | Ask first |
| 3 | Fix `_writer` underscore prefix on used parameter | Quick | Low | Ask first |

## Opportunity Details

### 1. Reorder h2 before quic/h3 in all feature/member lists
- **What**: Move `compio-h2` before `compio-quic` in workspace members, dependencies, feature lists, and `all` feature
- **Where**:
  - `Cargo.toml` (workspace root): lines 13,17-18 — move `compio-h2` before `compio-quic`
  - `compio/Cargo.toml`: lines 31-33 — move `compio-h2` dep before `compio-quic`
  - `compio/Cargo.toml`: lines 95-98 — reorder features: `h2` before `quic`, `h3`
  - `compio/Cargo.toml`: lines 110-113 — reorder `all` feature: `h2` before `quic`, `h3`
- **Why**: User requested ordering; h2 is more fundamental than quic/h3

### 2. Deduplicate SendData handler in connection.rs
- **What**: Lines 948-961 and 962-974 have identical `try_send_or_queue` + match logic. Refactor so the buffer-limit check only gates the error response, and the common `try_send_or_queue` path runs once.
- **Where**: `compio-h2/src/proto/connection.rs` lines 923-975
- **Why**: 10 lines of identical code. The else branch (lines 962-974) is a copy-paste of lines 948-961.
- **How**: Restructure the if/else so the error case returns early, then the common path follows unconditionally.

### 3. Fix `_writer` underscore prefix on used parameter
- **What**: In `handle_headers`, the parameter `_writer` (line 469) is prefixed with underscore despite being used on lines 552 and 559 to write RST_STREAM frames.
- **Where**: `compio-h2/src/proto/connection.rs` line 469
- **Why**: Underscore prefix in Rust means "intentionally unused" — misleading for a parameter that IS used.

### Considered and rejected

- **RST_STREAM helper extraction** (lines 550-562): Only 2 adjacent identical 2-line blocks. A helper saves ~2 lines but adds indirection. Not worth it.
- **Vec<StreamId> collection pattern** (6 occurrences): The `collect()` into Vec is needed because the loop body requires `&mut self` (via `get_mut()` / `close_recv_senders()`), which conflicts with the iterator borrow. This is the standard Rust workaround. Not worth changing.

## Execution Protocol
**DO NOT implement any changes without user approval.**
For EACH opportunity, use `AskUserQuestion`.
Options: "Implement" / "Skip (add to TODO.md)" / "Do not implement"
Ask all questions before beginning any implementation work
(do NOT do alternating ask then implement, ask then implement, etc.)
After all items resolved, run: `cargo test -p compio-h2`
